{
  "name": "knowledge-base",
  "version": "1.0.0",
  "description": "A static knowledge base site built with 11ty",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "lint": "eslint src --ext .js,.njk --ignore-pattern 'node_modules/'",
    "test": "node scripts/test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=18"
  }
}
